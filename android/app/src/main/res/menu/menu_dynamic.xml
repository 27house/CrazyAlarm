<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- 同级的item有几个则在toolbar右边显示几个 -->
    <!--
        id:之后设置点击事件的时候找到当前菜单项的唯一标识
        title:必填项，Toolbar栏的menu标题
        [如果存在icon，则显示icon图标，不显示文字]
        [如果不存在icon,则显示文字]
        app:showAsAction = "ifRoom" ：显示为一个行为
        android:showAsAction="ifRoom":icon失效显示图标为“3个竖立的点”
    -->
    <item
        android:id="@+id/dynamic_collection"
        android:icon="@mipmap/ic_collection"
        android:title="收藏" />

    <item
        android:id="@+id/dynamic_share"
        android:icon="@mipmap/ic_share"
        android:title="分享" />

    <item
        android:id="@+id/dynamic_delete"
        android:icon="@mipmap/ic_delete_1"
        android:title="删除" />
    <item
        android:id="@+id/dynamic_report"
        android:icon="@mipmap/ic_report"
        android:title="举报" />

</menu>